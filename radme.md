POC for comunication between mirofrontends

Run `docker-compose up app` to start.

Or if this doesn't work run it manually :
- sudo npm i -g lerna
- lerna bootstrap
- cd packages/dashboard
- npm run start